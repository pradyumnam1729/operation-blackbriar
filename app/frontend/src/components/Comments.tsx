// Placeholder — replaced by the Collaboration module build.
// Pages may already mount it: <Comments entityType="artifact" entityId={id} />
export function Comments(_props: { entityType: "artifact" | "request"; entityId: string }) {
  return null;
}
