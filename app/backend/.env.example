# Anthropic API key — required for /api/query and /api/assets/generate
ANTHROPIC_API_KEY=

# Model for PMM generation work. Default: claude-opus-4-8
PMM_MODEL=claude-opus-4-8

# Absolute path to the GTM War Room. Default: ../../GTM-War-Room relative to app/backend
WAR_ROOM_DIR=

PORT=3001

# Supabase (optional — app runs file-only without these).
# From supabase.com dashboard → Project Settings → API.
# Backend-only secrets: never commit, never expose to the frontend.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Microsoft Graph — SharePoint connector (optional FALLBACK).
# The primary path is the Integrations page in the app UI: an admin pastes the
# tenant ID, client ID, and client secret there and they are stored in the
# database (integrations row, kind sharepoint_credentials). Database values
# take precedence over these env vars. Azure Portal -> App registrations:
#   MS_TENANT_ID     = Directory (tenant) ID
#   MS_CLIENT_ID     = Application (client) ID
#   MS_CLIENT_SECRET = Client secret value
# API permissions: Microsoft Graph -> Application -> Sites.Read.All -> Grant admin consent.
MS_TENANT_ID=
MS_CLIENT_ID=
MS_CLIENT_SECRET=

# Jina AI Reader/Search key for the Competitive Intel module (jina.ai)
JINA_API_KEY=
